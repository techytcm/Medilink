/* appointments.js — basic dynamic filter */
(function () {
  const sel = document.querySelector('select[name="doctor_id"]');
  if (!sel) return;
  // Could fetch specializations and filter; minimal wiring here.
})();